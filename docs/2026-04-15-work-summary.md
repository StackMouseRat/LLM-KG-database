# 2026-04-15 工作总结（FastGPT 工作流联调 + Nebula 查询验证）

## 今日完成

1. 完成 FastGPT 工作流结构排查与节点链路核对  
- 核对了 `fastGPT_json/测试 Copy.json` 的节点体系、连线关系、循环子图及变量更新链路。  
- 明确当前主流程为：基础目录缓存 -> 二级故障匹配 -> 下游节点查询 -> 数据清洗 -> 最终生成。

2. 完成 Nebula 图数据库结构与子图能力验证  
- 实时查询确认 `llmkg_test` 空间结构：`entity`（含 `name/node_desc/lvl/source_id/degree/weight/stroke`）与 `rel(relation)`。  
- 验证 `GET SUBGRAPH WITH PROP ...` 可正常返回去重后的点边集合与关系属性。  
- 验证二级故障可达层级为 `3/4`，当前数据中无四级后续层级。

3. 完成查询语句与返回格式联调  
- 修复请求体键名问题（`space/ngql`），避免 `Space was not chosen`。  
- 将路径返回改为显式列返回，适配代码清洗节点。  
- 输出中已稳定包含关系字段（如 `relation_l2_to_l3`、`relation_l3_to_l4`）。

4. 完成 OpenAPI 调用排障与编码修复  
- 使用 `api/testkey.txt` 多轮调用 `POST /api/v1/chat/completions` 进行端到端测试。  
- 定位“后台 query 显示问号”根因为请求编码，改为 UTF-8 后已恢复中文正常显示。  
- `stream=true + detail=true` 流式响应可正常返回中文分片与节点状态事件。

5. 完成提示词与清洗逻辑迭代  
- 更新循环提取节点提示词，补充关系返回规则及 JSON 顶层键名约束。  
- 生成并验证“只返回单字符串 result”的清洗代码模板，支持故障原因/措施/现象/后果/风险/资源汇总。

## 当前问题

1. 最终生成节点间歇性空响应  
- 现象：`chat:LLM_model_response_empty`，`choices[0].message.content` 为空。  
- 范围：前置检索与清洗链路成功，问题集中在末端 chatNode 模型/提示词配置。

2. 二级故障匹配仍有误判风险  
- 输入“触头烧焦了”时，个别轮次会误判为非最优二级故障。  
- 需继续强化白名单硬约束与同义词锚点（触头烧焦/烧蚀/发热/接触不良 -> 触头及导电连接异常故障）。

## 建议下一步

1. 固化最终生成节点的最小可用配置（模型可用性 + 简化提示词 + 明确输入变量）。  
2. 为二级故障匹配节点增加程序侧白名单校验与自动纠偏。  
3. 增加“错误样例回放”测试集，形成稳定回归脚本（stream/非 stream 两套）。

